diff --git a/src/client/MarketSection.tsx b/src/client/MarketSection.tsx
index 5c17f4a..c12e633 100644
--- a/src/client/MarketSection.tsx
+++ b/src/client/MarketSection.tsx
@@ -206,6 +206,16 @@ function MarketLogo({ size = 16, style, animated = false }: { size?: number; sty
  */
 let cachedRegistry: Registry | null = null
 let cachedInstalled: InstalledMap | null = null
+const documentHotUrls = new Set<string>()
+const documentHotNames = new Set<string>()
+const documentRefreshNames = new Set<string>()
+
+/** Test isolation for module state that intentionally survives section remounts. */
+export function resetDocumentRefreshState(): void {
+  documentHotUrls.clear()
+  documentHotNames.clear()
+  documentRefreshNames.clear()
+}
 
 /** Discover grid page-size choices — the catalog grows daily, so cap each page. */
 const PAGE_SIZES = [24, 48, 96]
@@ -361,8 +371,8 @@ export function MarketSection(props: MarketSectionProps) {
   const [buildsSkipped, setBuildsSkipped] = useState<{ plugin?: RegistryPlugin; updateName?: string; names: string[] } | null>(null)
   const [updatingAll, setUpdatingAll] = useState(false)
   const [updatedNames, setUpdatedNames] = useState<string[]>([])
-  const [hotUrls, setHotUrls] = useState<string[]>([])
-  const [hotNames, setHotNames] = useState<string[]>([])
+  const [hotUrls, setHotUrls] = useState<string[]>(() => [...documentHotUrls])
+  const [hotNames, setHotNames] = useState<string[]>(() => [...documentHotNames])
   const [progressLine, setProgressLine] = useState<string | null>(null)
   /** Per-package activation states from /dsh-market/installed + operations. */
   const [activations, setActivations] = useState<Record<string, ActivationInfo>>({})
@@ -420,7 +430,7 @@ export function MarketSection(props: MarketSectionProps) {
    */
   const [restartNoticeDismissed, setRestartNoticeDismissed] = useState(false)
   /** Client-part plugins toggled this session — their UI needs a refresh. */
-  const [refreshNames, setRefreshNames] = useState<string[]>([])
+  const [refreshNames, setRefreshNames] = useState<string[]>(() => [...documentRefreshNames])
   const [envReady, setEnvReady] = useState(true)
   const [envFixing, setEnvFixing] = useState(false)
   const [envFailed, setEnvFailed] = useState(false)
@@ -787,8 +797,10 @@ export function MarketSection(props: MarketSectionProps) {
             // as pending-restart before the install response confirmed a hot
             // mount; a hot plugin must not stay in doneUrls (#73).
             setDoneUrls(urls => urls.filter(url => url !== plugin.url))
-            setHotUrls(urls => urls.includes(plugin.url) ? urls : urls.concat(plugin.url))
-            setHotNames(names => names.includes(plugin.name) ? names : names.concat(plugin.name))
+            documentHotUrls.add(plugin.url)
+            documentHotNames.add(plugin.name)
+            setHotUrls([...documentHotUrls])
+            setHotNames([...documentHotNames])
           } else {
             setDoneUrls(urls => urls.includes(plugin.url) ? urls : urls.concat(plugin.url))
           }
@@ -819,6 +831,11 @@ export function MarketSection(props: MarketSectionProps) {
       })
   }, [refreshInstalled, t])
 
+  const markRefresh = useCallback((names: readonly string[]) => {
+    for (const name of names) documentRefreshNames.add(name)
+    setRefreshNames([...documentRefreshNames])
+  }, [])
+
   /**
    * Restart the host and reload once the boot id changes (#14 by @ysyyhhh).
    * The 202 races the process's SIGTERM, so network errors on the initial
@@ -957,7 +974,8 @@ export function MarketSection(props: MarketSectionProps) {
       .then(res => res.json().then(body => ({ status: res.status, body })))
       .then(({ status, body }) => {
         if (status === 200 && body.ok) {
-          if (!body.hot) setRemovedCount(n => n + 1)
+          if (body.hot) markRefresh([name])
+          else setRemovedCount(n => n + 1)
           refreshInstalled()
         } else {
           if (body.cancelled === true) {
@@ -971,7 +989,7 @@ export function MarketSection(props: MarketSectionProps) {
       })
       .catch(error => setInstallError(String(error)))
       .finally(() => setRemovingName(null))
-  }, [refreshInstalled])
+  }, [markRefresh, refreshInstalled])
 
   /** Live enable/disable of one installed plugin (#60). `reload` opts the
    * card-level theme flow into a page refresh so the visual result lands
@@ -997,7 +1015,7 @@ export function MarketSection(props: MarketSectionProps) {
           if (body.restart === true) setToggleRestart(n => n + 1)
           // A client-part plugin's UI is already in the page — refresh to
           // show the change (mirrors the install hot banner).
-          if (body.refresh === true) setRefreshNames(names => names.includes(name) ? names : names.concat(name))
+          if (body.refresh === true) markRefresh([name])
           refreshInstalled()
           if (reload) {
             // Land back in the Themes tab with the stock look on screen.
@@ -1015,12 +1033,12 @@ export function MarketSection(props: MarketSectionProps) {
           // The durable state (state.json + patch layer) was still written,
           // so a restart applies it even though the live drive failed.
           if (body.restart === true) setToggleRestart(n => n + 1)
-          if (body.refresh === true) setRefreshNames(names => names.includes(name) ? names : names.concat(name))
+          if (body.refresh === true) markRefresh([name])
         }
       })
       .catch(error => setInstallError(String(error)))
       .finally(() => setTogglingName(null))
-  }, [refreshInstalled, t])
+  }, [markRefresh, refreshInstalled, t])
 
   /** Adopt the groups payload returned by POST /dsh-market/groups. */
   const setGroupPayload = useCallback((body: {
@@ -1051,7 +1069,7 @@ export function MarketSection(props: MarketSectionProps) {
             setToggleRestart(n => n + body.restartMembers.length)
           }
           if (Array.isArray(body.refreshMembers) && body.refreshMembers.length > 0) {
-            setRefreshNames(names => [...new Set([...names, ...body.refreshMembers])])
+            markRefresh(body.refreshMembers)
           }
           refreshInstalled()
           return true
@@ -1062,12 +1080,12 @@ export function MarketSection(props: MarketSectionProps) {
           setToggleRestart(n => n + body.restartMembers.length)
         }
         if (Array.isArray(body.refreshMembers) && body.refreshMembers.length > 0) {
-          setRefreshNames(names => [...new Set([...names, ...body.refreshMembers])])
+          markRefresh(body.refreshMembers)
         }
         return false
       })
       .catch(error => { setInstallError(String(error)); return false })
-  }, [refreshInstalled, setGroupPayload, t])
+  }, [markRefresh, refreshInstalled, setGroupPayload, t])
 
   const doGroupToggle = useCallback((name: string, enabled: boolean) => {
     return doGroupAction({ action: 'toggle', name, enabled })
diff --git a/src/index.ts b/src/index.ts
index 036f339..e748d3b 100644
--- a/src/index.ts
+++ b/src/index.ts
@@ -13,6 +13,10 @@ export const name = 'dsh-market'
 /** Optional cordis.yml configuration; profile defaults to `web`. */
 export type Config = Partial<Pick<MarketConfig, 'profile' | 'allowRestart'>>
 
+interface RestartSupervisorLike {
+  schedule(): { pid: number; helperPid: number | undefined; logOut: string; logErr: string }
+}
+
 /** Structural subset of DSH Desktop's public `desktopProfiles` contract. */
 interface DesktopProfilesLike {
   readonly current: {
@@ -50,9 +54,11 @@ export function apply(ctx: Context, config?: Config): void {
     const host = hostCtx as unknown as MarketEffectHost
     const desktopProfiles = ctx.get('desktopProfiles') as DesktopProfilesLike | undefined
     if (desktopProfiles === undefined) {
+      const restartSupervisor = ctx.get('dshMarketRestart') as RestartSupervisorLike | undefined
       const resolved: MarketConfig = {
         profile: config?.profile ?? argvProfile() ?? 'web',
         allowRestart: config?.allowRestart ?? true,
+        ...(restartSupervisor === undefined ? {} : { restartSupervisor }),
       }
       // Offer allowRestart as a switch on the settings page. Deliberately
       // NOT in the Desktop branch below: there the shell owns the process
diff --git a/src/restart.ts b/src/restart.ts
index 40995b0..f51c0fd 100644
--- a/src/restart.ts
+++ b/src/restart.ts
@@ -112,6 +112,11 @@ export interface RestartResult {
   logErr: string
 }
 
+/** Optional Cordis service supplied by hosts that own the process lifecycle. */
+export interface RestartSupervisor {
+  schedule(): RestartResult
+}
+
 /**
  * Relaunch this exact DSH entry after a short detached handoff, then stop
  * this process. The helper outlives us (detached + unref), waits for our
diff --git a/src/routes.ts b/src/routes.ts
index 7bf056a..c933657 100644
--- a/src/routes.ts
+++ b/src/routes.ts
@@ -32,7 +32,13 @@ import { isStaleUpdate, parseIgnoredBuilds, parsePrepareNotAllowed, RELEASE_AGE_
 import { checkUpdates, fetchNpmLatest, invalidateUpdates, isUpgrade, latestPublishedRecently } from './updates.ts'
 import { createThemeManager, type LoaderEntry } from './themes.ts'
 import { readJsonBody, sameOrigin, sendJson } from './http.ts'
-import { restartAllowed, scheduleRestart, trustedRestartRequest, trustedDownloadRequest } from './restart.ts'
+import {
+  restartAllowed,
+  scheduleRestart,
+  trustedRestartRequest,
+  trustedDownloadRequest,
+  type RestartSupervisor,
+} from './restart.ts'
 import { activationAfterReplace, hasHostHalf, verifyActivation } from './verify.ts'
 import {
   disableRow, enableRow, findUserPatchPath, isProtectedModule, packagePatchFlags,
@@ -72,6 +78,8 @@ export interface MarketConfig {
   profileDirectory?: string
   /** Detached self-restart is unsafe under systemd/launchd/pm2; operators can disable it (#14). */
   allowRestart?: boolean
+  /** Optional host lifecycle adapter; ordinary CLI deployments leave this absent. */
+  restartSupervisor?: RestartSupervisor
 }
 
 const PROFILE_RE = /^[A-Za-z0-9_-]+$/
@@ -1328,7 +1336,7 @@ export function mountMarketRoutes(
             setTimeout(() => {
               if (wantsRestart) {
                 try {
-                  const scheduled = scheduleRestart()
+                  const scheduled = config.restartSupervisor?.schedule() ?? scheduleRestart()
                   logEvent('info', 'self-uninstall', `restart scheduled pid=${String(scheduled.pid)}`)
                   return
                 } catch (error) {
@@ -1374,7 +1382,7 @@ export function mountMarketRoutes(
         }
         restarting = true
         try {
-          const result = scheduleRestart()
+          const result = config.restartSupervisor?.schedule() ?? scheduleRestart()
           logEvent('info', 'restart', `scheduled pid=${String(result.pid)} helper=${String(result.helperPid)}`)
           sendJson(response, 202, { ok: true, boot: BOOT_ID, ...result })
         } catch (error) {
@@ -1775,4 +1783,4 @@ export function mountMarketRoutes(
   return () => {
     for (const dispose of disposers) dispose()
   }
-}
\ No newline at end of file
+}
diff --git a/tests/client/market-section.client.spec.tsx b/tests/client/market-section.client.spec.tsx
index 4873a85..f24c37b 100644
--- a/tests/client/market-section.client.spec.tsx
+++ b/tests/client/market-section.client.spec.tsx
@@ -8,7 +8,7 @@
 
 import { cleanup, fireEvent, render, screen, waitFor, within } from '@testing-library/react'
 import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest'
-import { MarketSection } from '../../src/client/MarketSection.tsx'
+import { MarketSection, resetDocumentRefreshState } from '../../src/client/MarketSection.tsx'
 import { resetScreenshotsCache } from '../../src/client/market-data.ts'
 import { en } from '../../src/client/locales.ts'
 
@@ -70,6 +70,7 @@ afterEach(() => {
   cleanup()
   vi.unstubAllGlobals()
   sessionStorage.clear()
+  resetDocumentRefreshState()
 })
 
 describe('MarketSection (jsdom)', () => {
@@ -991,6 +992,7 @@ describe('status-poll / install-response race (#73)', () => {
       await vi.waitFor(() => { screen.getByRole('button', { name: en.tabInstalled }) })
       await vi.waitFor(() => {
         expect(screen.queryAllByText(re(en.restartBanner)).length).toBe(0)
+        expect(screen.getAllByText(re(en.hotBanner)).length).toBeGreaterThan(0)
       })
     } finally {
       vi.useRealTimers()
@@ -1027,6 +1029,26 @@ describe('uninstall confirmation Modal', () => {
     fireEvent.click(within(dialog).getByRole('button', { name: en.uninstall }))
     await waitFor(() => expect(fetchMock.mock.calls.some(([url]) => url === '/dsh-market/uninstall')).toBe(true))
   })
+
+  it('keeps the native refresh banner across section remounts after a hot uninstall', async () => {
+    stubFetch({
+      ...installedFixture,
+      '/dsh-market/uninstall': { ok: true, hot: true, installed: {} },
+    })
+    render(<MarketSection {...props()} />)
+    await screen.findByText('dsh-loop')
+    fireEvent.click(screen.getByRole('button', { name: /Installed/ }))
+    fireEvent.click(await screen.findByRole('button', { name: en.uninstall }))
+    const dialog = await screen.findByRole('dialog', { name: re(en.uninstall + ' dsh-loop?') })
+    fireEvent.click(within(dialog).getByRole('button', { name: en.uninstall }))
+    await waitFor(() => expect(screen.getAllByText(re(en.refreshBanner)).length).toBeGreaterThan(0))
+
+    cleanup()
+    render(<MarketSection {...props()} />)
+    await screen.findByText('dsh-loop')
+    expect(screen.getAllByText(re(en.refreshBanner)).length).toBeGreaterThan(0)
+    expect(screen.getByRole('button', { name: en.refresh })).toBeTruthy()
+  })
 })
 
 describe('per-tab search boxes', () => {
diff --git a/tests/flows.spec.ts b/tests/flows.spec.ts
index 696de04..33476dc 100644
--- a/tests/flows.spec.ts
+++ b/tests/flows.spec.ts
@@ -285,7 +285,14 @@ interface Testbed {
 }
 
 function createTestbed(
-  config: { profile?: string; allowRestart?: boolean; profileDirectory?: string } = {},
+  config: {
+    profile?: string
+    allowRestart?: boolean
+    profileDirectory?: string
+    restartSupervisor?: {
+      schedule(): { pid: number; helperPid: number | undefined; logOut: string; logErr: string }
+    }
+  } = {},
   runtime?: Parameters<typeof mountMarketRoutes>[2],
 ): Testbed {
   const routes = new Map<string, Handler>()
@@ -1218,6 +1225,25 @@ describe('one-click restart guards (#14)', () => {
     expect(restartCalls.count).toBe(1)
   })
 
+  it('delegates restart to a host lifecycle supervisor when supplied', async () => {
+    bed.dispose()
+    let supervisedCalls = 0
+    bed = createTestbed({
+      restartSupervisor: {
+        schedule: () => {
+          supervisedCalls += 1
+          return { pid: 42, helperPid: undefined, logOut: '', logErr: '' }
+        },
+      },
+    })
+
+    const r = await bed.dispatch('POST', '/dsh-market/restart', {})
+    expect(r.status).toBe(202)
+    expect(r.json.pid).toBe(42)
+    expect(supervisedCalls).toBe(1)
+    expect(restartCalls.count).toBe(0)
+  })
+
   it('refuses non-loopback peers, forwarded requests, and cross-origin posts', async () => {
     expect((await bed.dispatch('POST', '/dsh-market/restart', {}, { remoteAddress: '192.168.1.7' })).status).toBe(403)
     expect((await bed.dispatch('POST', '/dsh-market/restart', {}, { forwarded: true })).status).toBe(403)
diff --git a/tests/restart.spec.ts b/tests/restart.spec.ts
index 503be54..2be6d16 100644
--- a/tests/restart.spec.ts
+++ b/tests/restart.spec.ts
@@ -9,7 +9,11 @@
  */
 
 import { describe, expect, it } from 'vitest'
-import { respawnInvocation, trustedDownloadRequest, trustedRestartRequest } from '../src/restart.ts'
+import {
+  respawnInvocation,
+  trustedDownloadRequest,
+  trustedRestartRequest,
+} from '../src/restart.ts'
 
 const LAUNCH = { file: 'C:\\Program Files\\nodejs\\node.exe', args: ['--import', 'tsx/esm', 'bin.ts', '--profile', 'web'], viaShell: false }
 
