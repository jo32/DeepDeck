import Image from 'next/image';
import type { SiteLocale } from '../../lib/locale';
import { BenchmarkFoundations } from './benchmark-foundations';
import { BenchmarkComparison } from './benchmark-comparison';
import { BenchmarkEvidence } from './benchmark-evidence';
import { BenchmarkPilot } from './benchmark-pilot';
import s from './benchmark.module.css';

export function BenchmarkPage({ locale }: { locale: SiteLocale }) {
  const zh = locale === 'zh';
  const t = (cn: string, en: string) => zh ? cn : en;
  const home = zh ? '/zh' : '/';
  const experiments = zh ? '/zh/webmcp/experiments' : '/webmcp/experiments';
  return <div className={s.page}>
    <a href="#results" className={s.skip}>{t('跳转到实测对比', 'Skip to measured results')}</a>
    <header className={s.header}><div className={s.headerInner}>
      <a className={s.brand} href={home} aria-label={t('DeepDeck 首页', 'DeepDeck home')}><Image src="/brand/mark.svg" alt="" width={27} height={27} priority /><span>DeepDeck <b>Bench</b></span></a>
      <nav aria-label={t('产品导航', 'Product navigation')}><a href="#why-webmcp">{t('为什么是 WebMCP', 'Why WebMCP')}</a><a href="#results">{t('实测对比', 'Results')}</a><a href="#pilot">{t('评测交付', 'Deliverables')}</a></nav>
      <div className={s.headerActions}><a className={s.language} href={zh ? '/benchmarks' : '/zh/benchmarks'} hrefLang={zh ? 'en' : 'zh-CN'} aria-label={zh ? 'Switch to English' : '切换为中文'}>{zh ? 'EN' : '中文'}</a><a className={s.headerCta} href="#apply">{t('申请试点', 'Apply for a pilot')} ↗</a></div>
    </div></header>
    <main>
      <section className={`${s.hero} ${s.shell}`} aria-labelledby="bench-title">
        <div className={s.heroTop}><span className={s.eyebrow}><i /> COMPUTER USE, WITH A REFERENCE</span><span className={s.pilotBadge}>{t('WebMCP Ground Truth · 评测试点', 'WEBMCP GROUND TRUTH · PILOT')}</span></div>
        <div className={s.heroGrid}><div><h1 id="bench-title">{t('Agent 做得对吗？', 'Did your agent get it right?')}<br /><em>{t('用了多少 Token？', 'How many tokens did it use?')}</em></h1><p className={s.heroLead}>{t('比较成功率、Token 用量和完成任务的时间。', 'Compare success, token usage, and time to completion.')}</p><p className={s.heroCopy}>{t('我们先确认任务的正确答案，再用 WebMCP 尝试以较少步骤完成任务。把你的 Computer Use Agent 与它比较：是否答对、多做了哪些操作、多用了多少 token 和时间。', 'We first establish the correct answer, then use WebMCP to attempt the task in fewer steps. Compare your computer-use agent: did it answer correctly, which extra actions did it take, and how many more tokens and seconds did it use?')}</p><div className={s.heroButtons}><a className={s.primary} href="#apply">{t('申请试点评测', 'Apply for a pilot')} <span aria-hidden="true">↗</span></a><a className={s.secondary} href="#results">{t('查看三组实测对比', 'Explore three experiments')} ↓</a></div><p className={s.heroNote}>{t('面向 Agent 与模型团队 · 比较不同版本的实际表现', 'FOR AGENT & MODEL TEAMS · COMPARE VERSION RESULTS')}</p></div>
          <div className={`${s.heroScore} ${s.referenceHero}`}><div className={s.scoreWindow}><span><i /><i /><i /></span><span>BOOKS / 5 RUNS</span><span>{t('已完成实验', 'OBSERVED')}</span></div><div className={s.heroScoreBody}><p className={s.eyebrow}>SAME SUCCESS. DIFFERENT COST.</p><div className={s.scoreHeadline}><strong>{t('都答对了，消耗却不同。', 'Both correct. Different usage.')}</strong></div><p>{t('65 本书 → 最低价三本', '65 books → the three cheapest')}</p><div className={s.heroTable}><div><span>{t('每轮表现', 'Per-run results')}</span><b>CU</b><b>WebMCP</b></div><div><span>{t('完整成功', 'Full success')}</span><strong>5/5</strong><strong>5/5</strong></div><div><span>Token</span><strong>92,559</strong><strong>44,440</strong></div><div><span>{t('耗时', 'Time')}</span><strong>46.807 s</strong><strong>29.855 s</strong></div></div><div className={s.heroSavings}><div><strong>52.0% <span>↓</span></strong><p>{t('更少 token', 'Fewer tokens')}</p></div><div><strong>36.2% <span>↓</span></strong><p>{t('更少耗时', 'Less time')}</p></div></div><p className={s.heroReferenceNote}>{t('WebMCP 相对 CU · token 与耗时均为五轮中位数；实测参考，不是理论极限。', 'WebMCP vs CU · five-run medians for tokens and time; an observed reference, not a theoretical limit.')}</p></div></div>
        </div>
        <div className={s.valueStrip}>{[[t('任务能完成到什么程度', 'What can be completed'), t('先确认哪些任务已经可以完成', 'Check which tasks can be completed')], [t('Token 消耗差距', 'Token overhead'), t('统计完成任务一共用了多少 token', 'Count the total tokens used for the task')], [t('耗时与完成质量', 'Time & completion'), t('检查是否完成，再比较用了多久', 'Check completion, then compare elapsed time')]].map(([title, body], i) => <div key={title}><span>0{i + 1}</span><h2>{title}</h2><p>{body}</p></div>)}</div>
      </section>

      <BenchmarkFoundations locale={locale} />

      <section id="method" className={`${s.section} ${s.shell}`} aria-labelledby="method-title"><div className={s.sectionHeading}><div><p className={s.eyebrow}>GROUND TRUTH + REFERENCE EXECUTION</p><h2 id="method-title">{t('先检查答案，', 'Check the answer first.')}<br />{t('再比较消耗。', 'Then compare resource usage.')}</h2></div><p>{t('评测要分清两件事。第一，什么结果算正确，也就是 ground truth。第二，一种已验证的完成方法，需要多少步骤、token 和时间。只比较速度，可能把没做完的任务误认为更快。', 'An evaluation needs two things: a definition of the correct result, called ground truth, and a tested way to reach it with recorded actions, tokens, and time. Comparing speed alone can make an unfinished task look faster.')}</p></div>
        <div className={s.truthCards}><article><span>01 / GROUND TRUTH</span><h3>{t('什么结果算正确', 'What counts as correct')}</h3><p>{t('例如，三本书是否选对、价格是否正确、有没有按要求排序。每一项都检查，不能只凭答案看起来像是对的。', 'For example: were the right three books selected, are their prices correct, and are they in the requested order? Check each requirement rather than whether an answer looks plausible.')}</p><strong>{t('交付：正确结果和检查规则', 'DELIVERY: CORRECT RESULTS AND CHECKS')}</strong></article><article><span>02 / REFERENCE RUN</span><h3>{t('完成任务用了多少资源', 'What completing the task used')}</h3><p>{t('记录 WebMCP 是否完成任务、用了多少 token、从开始到结束花了多久。再与 CU 的结果比较。', 'Record whether WebMCP completes the task, how many tokens it uses, and the time from start to finish. Then compare those figures with CU.')}</p><strong>{t('交付：完成情况、Token 和耗时', 'DELIVERY: COMPLETION, TOKENS, AND TIME')}</strong></article></div>
        <p className={s.referenceDefinition}>{t('WebMCP 成功完成任务，说明这个任务在当时的条件下做得到。它用了多少资源，是一个实际比较值，不代表其他方法不可能做得更快或更省。', 'A successful WebMCP run shows that the task can be completed under those conditions. Its resource usage is an observed comparison point; another method may still be faster or use fewer tokens.')}</p>
      </section>

      <section id="results" className={s.resultsBand} aria-labelledby="results-title"><div className={s.shell}><div className={s.sectionHeading}><div><p className={s.eyebrow}>SUCCESS × TOKENS × TIME</p><h2 id="results-title">{t('比较成功率、', 'Compare success,')}<br />{t('Token 和耗时。', 'tokens, and time.')}</h2></div><p>{t('切换三组已完成实验，比较完整成功率、总 token 和耗时。既看是否完成，也看执行代价；完成量不同的运行，单独解释。', 'Switch between three completed experiments to compare full-task success, total tokens, and elapsed time. Inspect completion and execution cost together, with unequal-work runs explained separately.')}</p></div><BenchmarkComparison locale={locale} /><a className={s.evidenceLink} href={experiments}>{t('查看完整实验报告与运行条件', 'Read the full experiments and run conditions')} ↗</a></div></section>

      <section className={s.methodBand} aria-labelledby="design-title"><div className={s.shell}><div className={s.sectionHeading}><div><p className={s.eyebrow}>ONE TASK. TWO EXECUTION PATHS.</p><h2 id="design-title">{t('让两组完成同一任务，', 'Give both groups the same task.')}<br />{t('按同一要求检查。', 'Check the same requirements.')}</h2></div><p>{t('先约定网页数据、任务要求和运行限制。WebMCP 用工具完成任务，CU 用约定的页面操作方式完成任务。最后单独检查两组答案，并统计 token 和耗时。', 'Agree on page data, task requirements, and run limits. WebMCP uses tools; CU uses the agreed interface controls. Independently check both answers and count tokens and elapsed time.')}</p></div><div className={s.referenceFlow}><div className={s.flowSeed}><span>SHARED TASK STATE</span><strong>{t('同一快照 · 同一任务 · 同一成功标准', 'Same snapshot · same task · same success criteria')}</strong></div><div className={s.flowPaths}><article><span>REFERENCE / WEBMCP</span><h3>{t('运行 WebMCP', 'Run WebMCP')}</h3><p>{t('功能列表 → 文本模型调用 → 单独检查答案', 'Function list → text-model calls → independent answer checks')}</p><strong>{t('Ground truth · Token · 耗时', 'Ground truth · Tokens · Time')}</strong></article><article><span>AGENT / COMPUTER USE</span><h3>{t('运行你的 Agent', 'Run your agent')}</h3><p>{t('截图 / 无障碍树等约定能力 → UI 操作 → 结果', 'Declared screenshots / accessibility capabilities → UI actions → output')}</p><strong>{t('完成度 · Token · 耗时', 'Completion · Tokens · Time')}</strong></article></div><div className={s.flowOutcome}><span>↓</span><strong>{t('比较完成情况、Token、耗时，以及哪些地方没做对', 'Compare completion, tokens, time, and specific errors')}</strong></div></div><div className={s.methodNotes}>{[[t('裁判与 Agent 隔离', 'Keep the evaluator separate'), t('参考答案和裁判接口不向被测 CU Agent 开放。使用工具的参考组与 CU 组分别记录能力条件。', 'The CU agent cannot access reference answers or grading interfaces. Reference and CU tracks declare their different capabilities.')], [t('在同等交付下比较消耗', 'Compare costs at equal completion'), t('成功率与 token、耗时共同报告。模型、预算、环境版本和重试规则纳入协议，失败运行保留其实际消耗。', 'Report success alongside tokens and time. Declare model, budget, environment, and retries; retain resource usage from failed runs.')], [t('让参考结果经得起核对', 'Validate the reference itself'), t('用已知正确、错误和部分完成的状态验证校验器。环境异常与 Agent 失败分别记录。', 'Validate evaluators with known correct, incorrect, and partial states. Separate environment failures from agent failures.')]].map(([title, body]) => <div key={title}><h3>{title}</h3><p>{body}</p></div>)}</div></div></section>

      <section id="sample" className={`${s.section} ${s.shell}`} aria-labelledby="sample-title"><div className={s.sectionHeading}><div><p className={s.eyebrow}>COMPLETION BEFORE EFFICIENCY</p><h2 id="sample-title">{t('这次 HN 任务，', 'In this HN run,')}<br />{t('哪些做对了，哪些没做完？', 'what was correct and what was missing?')}</h2></div><p>{t('HN 的一次 CU 运行，100 条正文正确，只有 25 条回复关系得到确认。ground truth 要核对的是完整交付，才能让后续的资源比较有意义。', 'In one HN CU run, all 100 text entries were correct, but only 25 reply relationships were confirmed. Ground truth must check the complete deliverable for resource comparisons to mean anything.')}</p></div><BenchmarkEvidence locale={locale} /></section>

      <section id="pilot" className={s.pilotBand} aria-labelledby="pilot-title"><div className={s.shell}><div className={s.sectionHeading}><div><p className={s.eyebrow}>YOUR EVALUATION PACKAGE</p><h2 id="pilot-title">{t('你会拿到哪些', 'What will you receive')}<br />{t('评测材料？', 'from the evaluation?')}</h2></div><p>{t('为 Agent 团队、模型团队和企业自动化团队构建任务集。用同一套标准比较版本，判断能力是否进步，以及进步需要多少额外资源。', 'Build a task suite for agent, model, or enterprise automation teams. Compare versions with consistent criteria and see whether capability improves—and what that improvement costs.')}</p></div><div className={s.deliverables}>{[[t('任务集与 Ground truth', 'Tasks & ground truth'), t('网站能力地图、任务快照、目标字段、业务规则与经过验证的结果校验器。', 'Site capability maps, task snapshots, target fields, business rules, and validated outcome evaluators.')], [t('WebMCP 参考基线', 'WebMCP reference baseline'), t('经过验证的短路径、文本模型运行条件、逐轮完成度、token 与耗时；注明建图与初始化成本。', 'Validated short paths, text-model conditions, per-run completion, tokens, and time; explicit mapping and setup costs.')], [t('CU 差距报告', 'CU gap report'), t('成功率、资源差距、失败消耗与逐任务证据，支持版本对比。', 'Success, resource gaps, failed-run usage, and task-level evidence for version comparisons.')], [t('预算与回归评测', 'Budget & regression evaluation'), t('按约定预算重复评测，观察成功率与资源用量的变化，记录环境和版本。', 'Repeat evaluation under agreed budgets; track success and resource usage with environment and version records.')]].map(([title, body], i) => <div key={title}><span>0{i + 1}</span><h3>{title}</h3><p>{body}</p></div>)}</div><p className={s.pilotNote}>{t('当前为浏览器任务试点。任务范围、参考执行可行性、交付格式、维护期限与报价按需求确认。', 'Currently a browser-task pilot. Task scope, reference feasibility, delivery format, maintenance, and pricing are agreed per engagement.')}</p></div></section>

      <section className={`${s.faqSection} ${s.shell}`} aria-labelledby="faq-title"><div><p className={s.eyebrow}>READ THE METRICS CORRECTLY</p><h2 id="faq-title">{t('关于评测的', 'Questions about')}<br />{t('常见问题', 'the evaluation')}</h2></div><div className={s.faq}>{[[t("WebMCP 一定用最少的步骤吗？", "Does WebMCP always use the fewest steps?"), t("不一定。知道有哪些功能、怎么调用，通常能减少找按钮和试错。我们会记录实际找到并验证过的做法，但不把它说成唯一或最优的做法。步骤最少，也不一定最省 token 或最快。", "No. Knowing the available functions and how to call them can reduce searching and retries. We record methods that have been tested, without claiming they are the only or best methods. The fewest steps may not mean the fewest tokens or the least time.")], [t("为什么文本模型也能参加？", "Why can a text model take part?"), t("如果所需信息能用文字返回，操作能通过工具完成，模型就不必看懂截图。它仍需要理解任务、选择功能和检查答案。需要判断图片内容的任务，要用另外的评分方法。", "If information is returned as text and actions are available through tools, the model does not need to interpret screenshots. It must still understand the task, choose functions, and check the answer. Tasks that require judging images need a separate evaluation method.")], [t("这能说明 Agent 还有多少提升空间吗？", "Can this show where an agent can improve?"), t("能提供一个实际比较对象。WebMCP 做完而 CU 没做完，说明当时存在一种可行做法。两组都做完，就可以比较步骤、token 和耗时。差距可能来自可用信息、工具、规划或页面操作，不能全部算作视觉能力的差距。", "It provides a tested comparison. If WebMCP finishes and CU does not, a working method existed under those conditions. If both finish, compare actions, tokens, and time. Differences may come from available information, tools, planning, or interface actions—not vision alone.")], [t("Ground truth 是什么意思？", "What does ground truth mean?"), t("就是判断答案对不对的依据。例如，应该选中哪三本书、价格是多少、是否按要求排序。我们会另外核对这些答案，不会因为工具返回了一个结果，就直接认定它正确。", "It is the evidence used to judge whether an answer is correct: for example, which three books should be selected, their prices, and their order. We check these answers independently rather than assuming a tool result is correct.")], [t("Token 是什么，等于费用吗？", "What are tokens? Are they the same as cost?"), t("Token 是模型处理内容的计量单位，用来统计模型用了多少输入和输出。这里展示每轮累计用量，包含重复发送的内容。它不直接等于费用：模型价格、缓存和计费方式也会影响账单。", "Tokens are units used to count the content a model processes as input and output. We show accumulated run usage, including repeated content. Tokens do not directly equal cost: model prices, caching, and billing rules also affect the bill.")], [t("两组怎么比较才公平？", "How do you compare the groups fairly?"), t("尽量使用同一份网页数据、相同账户权限和任务要求，并记录模型、工具、重试次数等条件。准备工具和整理网站功能花了多少资源，也要单独记录。没做完的任务不能当成更快完成；网页或任务量不同时，也不能直接比较快了几倍。", "Use the same page data, account permissions, and task requirements where possible. Record models, tools, retries, and other conditions. Report the resources spent preparing tools and documenting site functions separately. An unfinished task is not a faster completion; different page states or task sizes do not support a direct speedup claim.")], [t("提交申请后会怎样？", "What happens after I apply?"), t("我们会通过你填写的邮箱联系你，确认要测哪些任务、比较哪些版本、交付什么结果，以及价格和时间。提交申请不会扣费，也不会自动开通服务。", "We will contact you at the email you provide to agree on tasks, versions, deliverables, pricing, and timing. Applying does not charge you or activate a service.")]].map(([q, a]) => <details key={q}><summary>{q}<span aria-hidden="true">+</span></summary><p>{a}</p></details>)}</div></section>

      <section id="apply" className={`${s.application} ${s.shell}`} aria-labelledby="apply-title"><div className={s.applyCopy}><p className={s.eyebrow}>BENCHMARK AGAINST WHAT’S POSSIBLE</p><h2 id="apply-title">{t('想评测你的 Agent？', 'Want to evaluate your agent?')}<br />{t('告诉我们要做哪些任务。', 'Tell us which tasks it should do.')}</h2><p>{t('填写目标任务、要比较的版本，以及你希望提高成功率、减少 token，还是缩短耗时。我们会与你确认评测内容。', 'Tell us the tasks and versions to compare, and whether you want higher success, fewer tokens, or less time. We will discuss the evaluation scope with you.')}</p><ol><li>{t('确定任务与成功标准', 'Define tasks and success criteria')}</li><li>{t('确认正确答案，运行 WebMCP 作比较', 'Establish correct answers and run WebMCP for comparison')}</li><li>{t('比较各版本的结果、Token 和耗时', 'Compare each version’s results, tokens, and time')}</li></ol><a href={experiments}>{t('查看已有实测证据', 'Read the experimental evidence')} ↗</a></div><BenchmarkPilot locale={locale} /></section>
    </main>
    <footer className={`${s.footer} ${s.shell}`}><a className={s.brand} href={home}>DeepDeck <b>Bench</b></a><span>GROUND TRUTH · TOKENS · TIME</span><div><a href={experiments}>{t('实验报告', 'Experiments')}</a><a href="https://github.com/jo32/DeepDeck">GitHub ↗</a></div></footer>
  </div>;
}
