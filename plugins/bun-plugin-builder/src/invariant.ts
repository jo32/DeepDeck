export const name = 'deepdeck-bun-plugin-builder-invariant'
export const inject: readonly string[] = []

/** The builder provides an additive service and owns no replacement invariant. */
export function apply(): void {}
