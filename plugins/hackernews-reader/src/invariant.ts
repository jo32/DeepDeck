export const name = 'deepdeck-hackernews-reader-invariant'
export const inject: readonly string[] = []

/** The reader owns no replacement service; its routes and tools are additive. */
export function apply(): void {}
