# DeepDeck presentation plugin. The upstream Agent preset plugin remains
# mounted and owns every behavior; this row only enhances its browser UI.
- insert:
    - id: deepdeck-agent-preset-sections
      name: '@deepdeck/dsh-client-ui-agent-preset-sections'
